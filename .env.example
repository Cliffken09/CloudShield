POSTGRES_USER=cloudshield
POSTGRES_PASSWORD=change_me
POSTGRES_DB=cloudshield

DB_CONNECTION=postgresql://cloudshield:change_me@localhost:5432/cloudshield
DOCS_URL=/docs

# Symmetric key used to encrypt vault secrets at rest. Generate your own —
# never reuse this placeholder:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=change_me

GRAFANA_ADMIN_PASSWORD=change_me
